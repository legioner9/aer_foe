# rh (remote_host)

    # sudo systemctl status ssh



# hh (home_host)

    ssh root@185.228.234.60 "df -h"
                            ". .init_ubuntu.f"
                            "ls -A /root/"

## удленная точка монтирования `sshfs пользователь@удаленный_хост:/удаленный/путь /локальная/точка/монтирования`

    hh#sshfs root@185.228.234.60:/root/hh.fs $HOME/aer_foe/.d/.sshfs/gb.sshfs





